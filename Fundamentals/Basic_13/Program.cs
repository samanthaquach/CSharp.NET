using System;
using System.Collections.Generic;

namespace Basic_13
{
    class Program
    {
        static void Main(string[] args)
        {
            // Console.WriteLine("Hello World!");
            // for (var i=1; i<=255; i++)
            // {
            //     Console.WriteLine(i);
            // }
            // for (var i=1; i<=255; i++)
            // {
            //     if (i % 2 == 1)
            //     Console.WriteLine(i);
            // }
            // for (var i=0; i<=255; i++)
            // {
            //     Console.WriteLine("New number " + i + " Sum " + i);
            // }
            // for (var i=0; i<x.length; i++)
            // {
            //     Console.WriteLine(x[i]);
            // }
            // var max = 0;
            // int[] arr = {-3, -5, -7};
            // for (var i=0; i<arr.Length; i++)
            // {
            //     if (arr[i] > max)
            //     {
            //         max = arr[i];
            //     }
            // }
            // int[] arr = {2, 10, 3};
            // var sum = 0;
            // for (var i=0; i<arr.Length; i++)
            // {
            //     sum+=arr[i];
            //     Console.WriteLine(sum/arr.Length);
            // }

            // int[] emptyIntegerArray = new int[0];
            // int[] Y;
            // int[] Y = new int[255];
            // List<int> YList = new List<int>();
            // for (int i = 0; i <= 255; i++)
            // {
            //     if (i%2 == 1)
            //     {
            //          YList.Add(i);
            //     }
            // }

            // int[] Y = YList.ToArray();
            // int[] arr = { 1, 3, 5, 7 };
            // int y = 3;
            // for (var i =0; i<arr.Length; i++)
            // {
            //     if (y > arr[i])
            //     {
            //         Console.WriteLine(arr[i], y)
            //     }
            // }
            // int[] arrX = { 1, 25, 100, 4 };
            // int[] arrY = { 1, 25, 100, 4 };
            // var sum = 0;
            // for (var i=0; i<arrX.Length; i++)
            // {
            //     sum = arrX[i]*arrX[i];
            // }
            // Console.WriteLine(sum);

            // int[] arrX = {1, 5, 10, -2 };
            // for (var i=0; i<arrX.Length; i++)
            // {
            //    if (arrX[i] < 0)
            //     {
            //         arrX[i] = 0;
            //     }
            // }

            // var min = 0;
            // var max = 0;
            // var sum = 0;
            // int[] arr = {1, 5, 10, -2};
            // for (var i=0; i<arr.Length; i++)
            // {
            //     if (arr[i] > max)
            //     {
            //         max = arr[i];
            //     }
            //     if (arr[i] < min)
            //     {
            //         min = arr[i];
            //     }
            //     sum+=i;

            //     Console.WriteLine(min, max, sum/arr.Length);
            // }

            // int[] arr = { 1, 5, 10, 7, -2 };
            // for (var i=0; i<arr.Length-1; i++)
            // {
            //     arr[i] = arr[i+1];
                
            // }
            // arr[arr.Length - 1] = 0;

            // int[] x = { -1, -3, 2 };
            // List<string> arr = new List<string>();
            // for (int i = 0; i < x.Length; i++)
            // {
            //     if ((int)x[i] < 0)
            //     {
            //         // x[i] = "Dojo";
            //         object ActuallyString = "Dojo";
            //         string ExplicitString = ActuallyString as string;
            //         arr.Add("Dojo");
            //     }
            // }
            // Console.WriteLine(x);


        }
    }
}
